.DS_Store
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.cache/
.coverage
htmlcov/
.venv/
venv/
build/
dist/
*.egg-info/

# 模型权重与运行缓存由脚本按需下载，不进入版本库。
/models/
/cache/

# CNN descriptors 是可由脚本重建的大型中间张量；公开仓库只保留汇总指标与图。
/outputs/cnn-analysis/descriptors/

# 可选 Picasso 本地比较会生成含原图像素的 CNN 输入与叠加图；只保留数值距离。
/outputs/cnn-analysis/activations/picasso-*/
/outputs/cnn-analysis/activations/las-meninas-2048-*/
/outputs/cnn-analysis/activations/geometry-100-50ee5089fe/

# 早期反向顺序（先 style 后 geometry）的试运行不属于冻结后的正式矩阵。
/outputs/combined-neural/

# `pics/` 含超大原图、来源未完整追溯的示意图与仍受保护的作品复制图。
# 公有仓库不重新分发这些本地研究素材。
/pics/
/data/raw/

# PowerPoint 构建过程使用仓库外临时目录；保留最终 PPTX 与完整渲染 QA，排除临时/中断产物。
*.pptx~
*.pptx.inspect.ndjson
/outputs/presentation/entering-las-meninas/
