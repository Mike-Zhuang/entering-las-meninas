# Parallax study

`entering-las-meninas-parallax-h264.mp4` 是最终作品的 4 秒、24 fps H.264 展示版。运动由明确标注的启发式纵向深度场驱动，仅用于让静态作品产生“向画内移动”的展示效果；它不是 CNN 单目深度预测，也不作为实验数据。

`entering-las-meninas-parallax.mp4` 是可视化脚本直接导出的 MPEG-4 工作版。H.264 版本由 FFmpeg 转码，便于浏览器与演示软件播放。
